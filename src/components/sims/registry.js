/**
 * The names of the available simulations, kept apart from the components so
 * that tooling (the curriculum checker) can read them without pulling in React.
 */
export const SIMS = ['hardware', 'desktop', 'window', 'files', 'browser', 'keys', 'scroll', 'ai']
